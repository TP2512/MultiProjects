{
  "validator": {
    "$jsonSchema": {
      "bsonType": "object",
      "required": ["username", "password"],
      "properties": {
        "username": {
          "bsonType": "string",
          "description": "Username must be a string"
        },
        "password": {
          "bsonType": "string",
          "description": "Password must be a string and not null"
        }
      }
    }
  },
  "validationLevel": "moderate",
  "validationAction": "error"
}
